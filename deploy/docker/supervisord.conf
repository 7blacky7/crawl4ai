[supervisord]
nodaemon=true

[program:redis]
command=redis-server
autorestart=true
priority=10

[program:uvicorn]
command=uvicorn server:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 65 --log-level debug
autorestart=true
priority=20